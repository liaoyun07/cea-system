# 新后端实施约定（Agent Rules）

作用域仅为 backend/。每次任务先读 README.md、docs/03-implementation-plan.md、docs/04-progress.md，确认当前范围、阶段和实际状态。文档记录不是对任意变更的自动授权，优先遵守用户当次要求，不自动进入下一阶段。

本文件约束工作方式，不替代技术事实文档，也不自动授权清理已有代码、字段或数据。

## 1. 核心原则与项目边界

本项目参考 Kestra 的领域模型、执行语义和组件职责，结合其架构设计思路重构本系统，不机械复制 Kestra。

设计或实现 Flow、Execution、TaskRun、Attempt、Executor、Worker、Scheduler、Queue、Repository、Retry、控制流等通用工作流能力时：

1. 先确认当前需求和已授权阶段。
2. 再参考 Kestra 当前官方文档或源码的做法。
3. 优先沿用成熟概念和职责边界，不自行创造平行模型。
4. 只实现当前功能真正需要的最小闭环。

Kestra 有，不代表现在就要做；Kestra 没有，也不要无理由自创。

保留以下项目边界：

- 合理模块边界优先于申报书一一映射。platform-offloading 仅用于原本在终端执行且允许卸载的任务；普通流程选址不自动使用卸载 DQN。
- 同级 web-platform/、amis/ 及既有数据库、服务、镜像不属于本工程的默认修改范围。
- 不复制旧模型兼容分支，不创建空业务类、不返回伪造成功。
- 项目模块只通过公开接口交互；禁止跨模块直接操作对方 Repository 或表。
- workflow-runtime 不依赖 platform-* 业务模块；实际需要的扩展通过接口和 server 装配，但不能据此提前创建 SPI。
- platform-foundation 不能作为绕过依赖规则的工具仓库。
- Git提交/推送按第6节的持续授权执行；修改Git历史、强制推送、启动或停止既有服务仍须用户单独授权。

## 2. 禁止过度设计

遵循 YAGNI 和最小可验证实现。新增以下内容，必须有当前功能中的明确消费者：

- checksum / hash。
- version / revision 辅助字段。
- idempotency key。
- metadata、状态字段。
- 数据库列或表。
- 缓存。
- 抽象接口 / SPI。
- 中间层 / Manager / Coordinator。
- 预留配置项。

新增前必须回答：

1. 当前哪个功能需要它？
2. 哪段生产代码会读取并使用它？
3. 它参与什么具体判断或行为？
4. 删除它会导致哪个当前测试或验收条件失败？

如果回答不了，不要加入。测试应验证真实功能或验收条件，不能只为证明新增结构存在而编写自证测试。

以下理由单独出现时不成立：“以后可能需要”“方便扩展”“更加完整”“最佳实践”“提高安全性”“先预留着”。必要的鲁棒性也必须对应当前具体风险和行为，不堆叠无消费者的防御性代码。

## 3. Hash / SHA-256 专项规则

不要默认给领域对象增加 hash/checksum。仅在以下场景使用：

- 明确的内容完整性校验。
- 内容寻址。
- 外部协议明确要求。
- 当前已实现的真实幂等逻辑。

同一业务对象不得存在多个意义相近的 hash/checksum，除非它们服务于不同协议边界且各自有明确消费者。

如果 checksum 只是“算出来、存进去、再读出来”，但不参与任何当前业务判断，应删除。对已有实现的删除仍须符合当次授权范围，并检查接口、数据迁移和测试影响；不能因更新本规则而擅自删除。

## 4. 参考 Kestra 的规则

实现通用工作流能力前，先回答：

1. Kestra 如何定义这个概念？
2. Kestra 中谁拥有这个状态？
3. Kestra 的调用或消息边界是什么？
4. 本项目是否需要同样复杂度？

如果本项目与 Kestra 不同，必须说明差异属于哪一种：

- 云边端业务需求差异。
- 当前阶段的有意简化。
- 技术约束。

不要因为 Kestra 存在某个服务、模块、表、字段或接口，就提前复制。优先参考职责边界和执行语义，而不是文件数量、服务数量或类名。

本地参考源码位于 D:/Project/Kestra/kestra。记录实际参考的文件及版本/提交（可获取时）或官方文档链接；本地源码不自动等于当前官方版本。不能凭印象声称“与 Kestra 一致”；资料不可用时说明限制，不把推测当作已核实结论。

## 5. 修改代码前

每次任务先检查：

- 当前功能范围、已授权阶段和验收条件。
- 相关现有实现，避免重复造轮子。
- 涉及通用工作流能力时，Kestra 对应能力的成熟设计。
- 是否引入新的状态所有权、表、字段或跨模块依赖。
- 新增结构是否满足第2节的消费者和行为检查。

重大架构变化先说明方案和原因，记录 ADR，再在已授权范围内实施。需要扩大范围或改变尚未确定的业务语义时，先讨论，不用“架构优化”代替授权。

## 6. 修改代码后与完成条件

必须说明：

- 改了什么，为什么改。
- 主调用链是否变化。
- 新增或删除了哪些类、字段、表或接口。
- 涉及通用工作流能力时，与 Kestra 的做法是否一致；若不同，为什么。
- 当前新增了什么真实能力，哪些能力仍未实现。
- 对应测试是否通过，哪些未测或失败。

不涉及某项时明确“不涉及”，不用为了填满说明而制造设计或改动。不要用“框架已搭好”“已支持扩展”“预留完成”等表述冒充功能完成。

每个实现批次还须满足：

1. 在实施计划中明确任务编号、范围、依赖和验收条件。
2. 更新功能文档的语义、边界和测试，不只更新状态。
3. 新增、删除或移动生产 Java 文件时同步 docs/01-code-architecture.md。
4. 影响接口、事件或数据结构时更新 docs/contracts/；重大取舍写入 docs/decisions/。
5. 执行相关自动化测试，在 docs/verification/ 记录实际结果、环境和源码版本；未测不能写通过。
6. 同步 docs/04-progress.md 与 docs/02-feature-index.md；计划变更保留原因，不能通过删掉验收条件冒充完成。
7. 运行 scripts/check-scaffold.ps1；需要构建时用 JDK 21 执行 scripts/verify.ps1。完整 verify 包含真实 MySQL 集成测试，需要 Docker；结构检查不能替代业务测试。

仅文档任务按实际影响更新相关文件并验证，不为凑齐记录而改动无关功能状态，也不把未重跑的历史业务测试写成本次通过。

### GitHub更新约定（2026-09-10用户持续授权）

以后完成较大的新增或修改（例如可独立验收的功能批次、跨模块改动或重要修复），在对应测试通过、文档同步后，主动提交并推送到新backend的GitHub仓库，不再逐次询问。小型零散调整可随下一次相关功能提交；用户当次要求不提交/不推送时优先遵从。

- 当前授权目标为公开仓库liaoyun07/cea-system的main分支；提交前核对仓库根、分支和origin，不自动更换远端或扩大到旧工程。
- 检查待提交差异，只纳入本批相关代码、测试、示例和文档，保留并排除无关未提交修改；不得上传真实凭据、敏感数据、构建产物或本地缓存。
- 使用普通提交和非强制推送，不重写历史。若测试未通过、存在敏感内容、分支分歧或推送失败，保留工作并明确报告原因，不绕过检查、不伪称已同步。
- 推送后核实远端提交与本地一致，最终说明提交号、仓库链接和实际验证结果。完成一个子批次可以同步，但不得因此把整个阶段标为完成。

## 7. 文档维护与面向项目所有者的说明

技术事实继续以现有文档为准：

- docs/00-overview.md：系统总览和职责边界。
- docs/01-code-architecture.md：实际项目结构、Java 文件和调用/事务职责。
- docs/02-feature-index.md：功能实现与验证状态。
- docs/03-implementation-plan.md：阶段、依赖、验收和计划变更。
- docs/04-progress.md：当前事实、问题和下一步。
- docs/features/、docs/contracts/、docs/decisions/、docs/verification/：功能语义、协议、决策与验证证据。
- docs/05-documentation-guide.md：文档维护和可追溯规则。

面向项目所有者的说明保持简单，重点说清：

1. 当前系统实际能做什么。
2. 当前真实调用链。
3. 当前没有实现什么。
4. 本次修改带来了什么行为变化。
5. 下一步准备解决什么。

不要把目标架构写成当前已实现能力，不重复维护多份相互冲突的技术事实。

## 8. 最终判断标准

遇到任何“看起来更专业”的设计，先问：

> **如果现在删掉它，哪个当前功能会坏？**

如果答案是“不会”，默认不要做。目标不是做出最复杂的架构，而是：

> **用 Kestra 已验证的思路，做出当前需求所需的核心、清晰、可恢复、可测试实现。**
