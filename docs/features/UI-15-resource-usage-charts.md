# UI-15 节点资源用量图形化

状态：DONE，2026-09-18 00:33已部署CEA前端，真实接口及桌面/窄屏核验通过。

UI-15a：2026-09-18 00:55已部署节点表精简；56项单测、77项浏览器及四集群/窄屏现场核验通过。

范围：沿用UI-08的Kubernetes节点指标，只替换前端CPU/内存摘要；不改采集、资源调度、API、数据库或执行链。

- 两张SVG环形卡片：CPU紫色、内存青色；中心为真实百分比，旁边为全部节点累计已用核数/GiB。无图表依赖，无额外轮询或网络请求。
- 分母继续为节点总容量，不是可分配容量、资源requests/limits或应用进程内存。节点表保留CPU/内存总容量、可分配容量及用量；UI-15a按用户要求隐藏Pod上限和采样状态/时间/窗口，不删除后台有效性判断。封锁调度继续为只读外部节点状态，不新增封锁/解除入口。
- 百分比直接读取后端的全节点加权结果；已用量加总同一返回值的全部节点，不以当前分页或百分比简单平均冒充集群汇总。
- 缺失、过期和错误显示“—/用量不可用”，刷新时清空旧值；真实0显示0，超100%的数值保留、圆环最多一圈。不增加告警阈值或伪造剩余内存。
- 标明内存为工作集、包含部分文件缓存；这不是宣称不可回收内存或所有业务容器占用。
- 桌面并排、窄屏竖排；图形提供文字读数及可访问名称，不靠颜色独立传达数值。

参考旧页面`amis/pages/public/node/node-single-all.json`的SVG圆环结构，保留CEA配色和交互，不引入旧AMIS组件。只读诊断的新旧采集口径和同机K3s边界见[验收](../verification/VER-UI-015-resource-usage-charts.md)。本批没有新增生产组件、Java类、字段、表、HTTP接口或状态所有权；不涉及Kestra通用执行语义，不修改四集群部署/cgroup边界。
