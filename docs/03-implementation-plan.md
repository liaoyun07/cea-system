# 可修改的实施计划

计划版本：0.35。建立日期：2026-09-08。实际状态只在[当前进度](04-progress.md)维护；本文件记录计划、依赖、退出条件与变更历史，不维护第二份完成百分比。

## 当前授权与实施原则

2026-09-13 UI-08-demo：用户要求准备可部署的示例镜像。仅增加常驻HTTP示例、契约/部署参数、构建导出脚本和真实容器测试；依赖现有上传与Deployment能力，不新增平台Java/API/表/执行逻辑，不向CEA登记或部署、不修改已有数据。验收默认命令非root启动、8080健康检查、实际数值统计/错误响应和单镜像tar导出，镜像供用户自行上传部署。

2026-09-13 UI-08发布授权：用户确认“部署”。依赖63fda36已验收产物；只更新CEA前后端，应用V18/V19新增表、上传暂存卷，以及四个集群的Metrics Server和受限只读RBAC。发布前确认无活动Execution/afterExecution，备份专用数据库、配置、回退镜像与业务/容器快照；发布后验证健康、迁移、四集群采样和实际18080页面，比较既有Flow/历史及其余10容器不变。不重新seed/训练、不修改旧系统，不把此次上线视为30秒或物理多云性能达标。

2026-09-12 UI-08：用户确认实现按需镜像分发历史、镜像归档上传、安全部署编辑/手动扩缩容及真实部署耗时、近期CPU/内存用量展示。按分发记录→上传→部署→资源用量顺序完成核心闭环；复用现有服务与执行链，新增两张deployment记录表，不新增通用操作中心、上传会话或时序库。日志、产物下载、数据上传、在线构建、HPA、告警和调度/DQN不在范围。不发布CEA、不修改存量Flow/执行或旧系统；真实验证使用隔离容器。验收包括摘要核对、不可变版本、权限隔离、配置往返/CAS、同代就绪计时与无效计量、资源采样/缺失、真实浏览器及全量回归。设计见[ADR-0020](decisions/ADR-0020-core-deployment-operations.md)，规格见[UI-08](features/UI-08-core-deployment-operations.md)。

2026-09-12 UI-07发布授权：用户确认“部署”；仅更新CEA前后端及四个集群中既有Role/ClusterRole的受限只读规则，不重启其余10个容器，不修改Flow/Execution历史，不重新seed或训练。发布前检查无活动执行/afterExecution并保存回退镜像、业务和容器快照；发布后以实际18080页面及API复核，并比较历史与其余容器不变。

UI-07实施审查补充：普通Flow读取API限定USER管理范围，不能用来查询EDGE_POLICY执行的产物声明。增加只读output-files，从已有Execution.definition取任务ID/端口；产物页和Metrics共享此真实消费者。原有两种管理范围保持隔离，无“先查Flow、失败再查策略”的兼容回退。当前批次共5个新增GET，无新Java文件或表（相对UI-07既定2个类）。增加两种管理范围、旧快照与新Flow修订的回归。

2026-09-12 UI-07：用户选择优先迁移第1/2/4项：任务产物JSON查看、执行总览、Node/Service/Kubernetes Namespace只读资源。依赖UI-06成功产物读取、runtime Execution持久化、resource目录和显式Kubernetes连接。总览按数据库时间界定UTC日窗口、全量聚合状态及日计数，不以列表分页代替总数；资源仅授权目录集群，Service/Namespace限制到配置的Kubernetes命名空间。验收身份/作用域/时间边界/分页、真实MySQL与K3s及S3、浏览器桌面/窄屏和回归。不新增状态表/字段、调度动作或执行链；不实施Pod日志、资源利用率、任意对象存储浏览或DQN/计量。实现范围见[规格](features/UI-07-readonly-inspection.md)；CEA更新和只读RBAC应用须另获当次授权。

2026-09-12 UI-06：补Metrics展示，先查Kestra源码。依赖已有TaskRun/成功Attempt和JSON产物；仅新增受READ权限、固定执行修订、精确产物地址及256KiB约束的JSON读取接口，前端提供任务/产物/数值指标选择、实例表和单指标图。无需新表、SDK或算法改造；不扩大MET-001/DQN/Pod日志/全局产物管理。验收真实S3/Job、FedAvg/FedProx两轮原值、身份隔离、非法文件、空/零值、多轮/item、历史修订及桌面/窄屏。后续用户当次明确授权发布CEA前后端：仅更新这两个服务，不改存量Flow和历史，其它10服务不重启；使用已有执行做只读上线验证。

2026-09-12 UI-05：先补已有后端数据的展示：只读执行拓扑、TaskRun输出/开始结束/实例耗时/尝试，FedAvg和FedProx数据集输入显式SELECT。参考Kestra拓扑与实例详情，不新增图API/状态机/计量或输出内容解析。验收历史修订、Dag依赖、Parallel、Repeat/Loop动态实例隔离、失败/取消/重试、真实浏览器与算法回归、桌面和窄屏。用户当次确认：通过后仅发布CEA前端及两个Flow的CAS新修订；保留其它配置/历史，不重启其它11个服务。见[规格](features/UI-05-execution-inspection.md)。

2026-09-12 UI-02d：用户仅授权将Loop.values固定集合的每一项改为一个JSON文本框，保留逐项添加/删除/排序、来源切换及现有DSL。依赖UI-02b/c；不实施上一轮讨论中的Flow.inputs入口、统一任务Schema或其它编辑器重构。验收对象/嵌套数组/标量类型往返、无递归控件、非法JSON保留草稿并阻止保存、增删排序及真实保存执行、桌面/窄屏。用户已当次确认测试后只更新CEA前端，不改已存Flow或其它服务。

2026-09-12 UI-02c：按用户参考图调整任务参数排版，type/id及类型必填字段前置，Loop.values等直接展示，其他适用配置纵向排列；避免以标签切换表现为并列功能。依赖同源No-code与已有Binding，不新增后端字段/状态或执行语义。验收字段顺序/必填标记、真实保存回读、引用/固定值与既有回归、桌面/窄屏。用户已当次确认测试后仅更新CEA前端，不改已存流程，不重启后端或其他服务。

2026-09-12 UI-04：用户要求增加Kestra方向的SELECT输入。依赖现有Flow Input/Binding、Schema和同源编辑，最小实现固定字符串values、单选、defaultValue/required及保存/预览/所有正常提交入口的成员校验；No-code配置选项/默认值，执行表单用下拉框。验收YAML与JSON、非法选项/默认值、修订/触发、真实浏览器保存执行及既有回归。不增加动态表达式、多选、自定义值、表/API/SPI或第二套绑定。用户已确认测试后更新CEA前后端，不修改现有Flow，不重启数据库、Registry或算法集群；发布前检查活跃Execution。

2026-09-12 UI-02b：按Kestra的类型切换/逐项添加交互编辑Loop.values，沿用LITERAL/INPUT/VARIABLE/TASK_OUTPUT，不增加表达式或后端状态。FedAvg/FedProx将clients移到Loop固定集合，保持ITEM绑定与训练/聚合/评估/反馈。验收对象/数组/类型/增删排序、来源切换、YAML往返、真实保存执行、无clients启动参数与真实算法回归。用户已确认测试后更新CEA前端及两个已存流程的CAS新修订，不覆盖其他配置或历史版本，不重启后端、数据库或算法集群。

2026-09-11 UI-02a：修复候选集群点击缺少选中反馈。依赖UI-02既有同源YAML编辑；按钮紧邻候选列表，提供选中高亮/勾选和再次点击取消，状态直接来自当前Flow，不新增选择缓存、Binding、API或调度逻辑，不添加帮助说明。验收初始回显、多选/取消、源码同步、保存回读、禁用集群及窄屏显示；完成相关回归和文档追溯。CEA前端发布需当次确认，不修改生产流程或其他服务。

2026-09-11 UI-03a：按用户要求直接删除新前端的常驻说明文字，不改为展开帮助或悬浮提示。依赖UI-01至03；保留字段标签、实际数据、错误、确认和操作结果，不改API或执行逻辑。验收各页无说明段落、原编辑/提交/查询回归通过；随后用户明确确认部署，只更新CEA frontend并做只读复核，不重启其他服务。

2026-09-11用户授权UI-03：迁移老页面中已有新后端支持的管理功能。依赖UI-02及S4/S5公开接口，增加应用契约/镜像准备、常驻部署创建查询删除、集群与数据集目录、网关/终端登记启停、同源策略编辑及卸载观测只读页面。只改新前端，不增加Java/API/数据库或旧DTO兼容；部署更新接口缺少完整配置回读，因此不提供可能清空参数/命令的快捷启停/缩放。验收真实API浏览器登记/查询/不可变冲突/策略CAS和隔离、错误显示、既有No-code回归；部署/镜像外部操作单独注明实测范围。用户已允许通过后仅更新CEA frontend，不重启其他服务。详见[UI-03规格](features/UI-03-management.md)。

2026-09-11用户授权UI-02 No-code：参考Kestra的任务块/表单/源码联动，覆盖当前Flow任务和生命周期字段，显式Binding、应用/集群/数据集选择、校验/CAS保存/固定修订执行及修订查看回退。只编辑同一份YAML，不新增Java、表、API或执行链；不实现Pod日志、指标、局部重跑或新的DSL。分为02a文档节点操作与往返测试、02b可视化任务组和Schema表单、02c真实API浏览器回归及文档发布。验收覆盖FedAvg/FedProx配置往返、动态作用域、无效YAML和未覆盖字段保留、目录失败、移动/删除引用检查、并发保存冲突及真实执行。详见[UI-02规格](features/UI-02-no-code.md)。

2026-09-11用户授权DEPLOY-01：Docker数据盘迁至D盘后，立即建设独立Compose项目cea，配置文件/数据库/存储/四个Registry/一个云与三个边缘K3s及前后端闭环，真实运行FedAvg/FedProx。依赖S4/S5联邦算法与UI-01，验收专用空库迁移、最小凭据/权限、镜像真实分发、两轮训练聚合评估、前端可查与重启持久性；不导入旧数据、不切换旧系统，不以单机多集群冒充物理多云，不实施计量或DQN。属于OPS-001独立部署子批，保留S7旧迁移/切换原退出条件。

2026-09-11用户授权独立前端最小闭环，追加要求风格/操作参考Kestra。UI-01依赖S6既有协议，在仓库frontend目录独立构建，按[工作包](features/UI-01-console.md)完成列表、YAML编辑/CAS保存、固定修订输入与提交、执行/任务/日志/结果及真实浏览器联调。不修改旧AMIS，不新增Java/表/API/执行链，不自动进入S7、No-code、计量或DQN。

2026-09-10用户授权继续完成S6剩余02–04。依赖已验收的S6-01及S4运行链；实现小型文本Namespace Files固定修订与真实Application消费、统一Checks和认证Webhook、maxDuration告警与原Executor持久afterExecution，验收权限/CAS/固定版本/接管、所有提交入口、重投、后处理失败与主结果隔离、取消/故障及完整verify。范围见[ADR-0019](decisions/ADR-0019-s6-files-lifecycle.md)。不进入完整前端、S7、计量或长期DQN。

2026-09-10用户授权开始S6；按[工作包](features/S6-editor-management.md)分01编辑/流程管理、02文件、03触发/检查、04时限/后处理逐批验收。先实现01，不以单批完成冒充全S6。用户已同意后置S5-05数据速率及DQN研究，保留原验收缺口；不进入S7或完整前端工程。

2026-09-10用户授权“先做最小解耦，然后推进S5-05”。S5-04c仅解开普通CLUSTER/固定TERMINAL执行和资源回收对卸载观测的依赖；保留既有RULE/单步Q和历史数据，不扩大卸载策略/候选范围模型、不建设多步DQN。验收普通与终端任务成功、失败、取消、接管和容量释放时不需要offloading观测。S5-05先确认算法区间/字节/并行计量口径，再实现SDK与后端汇总；不因推进阶段而默认为性能指标达标。

具体后续批次、拟议复用路径及计量待决条件见[S5-05计划](features/S5-05-measurement.md)。未确认口径前只进行设计，不落地计量字段/SDK/接口。

2026-09-10用户授权“继续S5-04剩下的部分”。实施04b：显式终端卸载资格、规则/单步Q模型、普通应用也可贡献的实际画像、反馈与终端容量FIFO。依赖04a及既有资源/应用/Worker链，新增dataflow→offloading公开服务依赖。验收三位置真实执行→真实样本训练注册→模型执行、权限/隔离、画像与反馈、FIFO并发/取消、既有恢复和全量回归。单步episode是本批有意简化，不宣称长期DQN或优于规则；不删除S5原退出条件，不进入计量或S6/S7。见[ADR-0016](decisions/ADR-0016-terminal-offloading.md)。

2026-09-10用户授权继续S5-04并确认终端安装Docker。分a终端真实执行/接入权限闭环、b允许卸载及规则/DQN/画像/反馈逐批验收；a不代表完整卸载。先修复S5-03真实Application鉴权缺口，复用同一Application/Worker和文件协议，不做另一套终端执行状态机。详见[ADR-0015](decisions/ADR-0015-terminal-docker.md)。原S5-04退出条件保留，不进入S5-05。

2026-09-10用户授权S5-03：网关/终端正常接入、策略管理、统一提交及结果查询，设计和验收见[ADR-0014](decisions/ADR-0014-edge-access.md)。依赖已完成S4及S5-02b，不进入S5-04/05。

2026-09-10用户批准S5-02b：通用core.Loop、ITEM Binding、动态候选集群及集合文件输入，改写FedAvg/FedProx为一份train。依赖已完成S5-01/02；验收包括有界并发、顺序输出、轮次/实例隔离、失败/重试/取消/重启、真实两轮数值回归。见[ADR-0013](decisions/ADR-0013-loop.md)及[协议](contracts/s5-loop.md)。不增加第二套执行链或业务状态表，不进入S5-03至05。

2026-09-10用户明确本批只迁移FedAvg和FedProx；S5-02采用既有Application/Repeat和文件产物链，其他流任务（TEP、预处理、分层联邦等）暂不迁移，不同时进入S5-03/04/05。不修改旧服务、数据库或已发布镜像，不实现计量SDK。

2026-09-10用户授权开始S5。按[S5工作包](features/S5-research-edge.md)分为01 Repeat、02真实联邦学习验证、03网关/终端与策略、04终端卸载、05计量。首批实现01，设计见[ADR-0011](decisions/ADR-0011-repeat.md)；其余不提前标完成。计量候选仍待讨论，不因授权开始阶段而擅定科研指标；S6/S7未进入。

2026-09-10继续S4-03/04/05：显式Application Task、Job文件传输、同Attempt接管、资源观测/平台槽预约和取消回收，设计见[ADR-0009](decisions/ADR-0009-s4-job-execution.md)；GET/POST、只读SQL及容器内Shell/Python见[ADR-0010](decisions/ADR-0010-s4-common-tasks.md)。保留进程/DB短时故障和真实JAR启动验收；不以完成一个子批次替代全阶段。

2026-09-10用户授权完成S4剩余部分，覆盖S4-02b/c、S4-03、S4-04及S4-05，取代上批仅修正S4-02a的实施限制。显式Flow边界仍有效；按真实消费者逐批实现/验收，未通过不标完成。设计见[ADR-0008](decisions/ADR-0008-s4-external-runtime.md)。

S4原授权按[S4工作包](features/S4-resource-runtime.md)逐批验收，S4-01设计见[ADR-0006](decisions/ADR-0006-s4-resource-boundary.md)；现S1–S4均为已验收基线，当前进入S5，不减少S4原退出条件。2026-09-10用户持续授权：以后完成较大的新增或修改并验证后，按[AGENTS.md](../AGENTS.md)主动提交并更新GitHub；不扩大实施阶段。

2026-09-10用户修正S4-02a方向：只保留应用契约目录，撤销自动派生Flow Inputs及独立alias/binding。Flow作者显式定义输入和Task来源，未来YAML/No-code共用Flow；等真实Application/Container Task可执行时再实现参数映射闭环。该历史子批仅修正S4-02a；现已由上方最新授权推进剩余S4。仍禁止无消费者的新模型/表/SPI。原镜像准备/分发及常驻部署退出条件保留。设计见[ADR-0007](decisions/ADR-0007-application-contract-binding.md)。

S3已完成，backend独立仓库的起始提交为071ff4b。S4先完成目录和本地性检查，再按实际运行消费者接入镜像/Job/预约，不提前创建空SPI。P04/P05/P06/P11随真实接口及运行环境落实，不能仅注册资源就标为保障完成。新后端统一Java21。

范围明确调整：不做终端任务断线后的自动恢复/补发/断点续跑及恢复专用ACK；正常终端接入、卸载、结果返回仍在S5，S2服务端Worker接管不受影响。数据处理速率口径到S5开始时再确定。生产保障已确认：P01/P07保留现状，P04/P05/P06/P11最小实现，其余现在后置；具体边界和验收见[范围清单](06-deployment-safeguards-review.md)。P10后置不取消S3核心并发/FIFO，P09后置不删除既有迁移检查。

采用独立 backend 工程，不修改旧服务。按最小可验证闭环递进；不一次性建齐所有目标类，不以空接口、固定成功结果或兼容开关充当完成。结构选择服从业务边界，申报书通过功能映射体现。

## 阶段与退出条件

| 阶段 | 依赖 | 范围及关联编号 | 必须满足的退出条件 |
|---|---|---|---|
| S0 工程与文档框架 | 无 | FND-001；8模块、Java21、文档索引、计划/进度/ADR/验证规范 | 文档结构检查通过；JDK21 Maven verify 通过；没有业务实现；记录“0业务测试”与环境 |
| S1 定义和最小执行闭环 | S0 | WF-001–006、SEC-001基础；选择 Boot/BOM、MySQL迁移；单 Log 顺序流程 | 保存和版本不覆盖；同键提交幂等；消息与状态同事务；查询真实日志/结果；进程重启不丢已提交请求 |
| S2 持久恢复和失败语义 | S1 | WF-007–008；Worker协议、租约/epoch、重试/取消、Errors/Finally | 重复/过期结果不回退；Worker断开不盲目新建业务attempt；失败/清理错误可区分；实际故障测试通过 |
| S3 完整通用控制流 | S2 | WF-009–011；DAG/条件/并行/并发/定时 | 乱序定义与并发结果正确；分支恢复确定；双触发器不重复；限流/取消/失败组合不死锁 |
| S4 资源与运行环境接入 | S2/S3 | RES-001–002、DEP-001–002、RUN-001、WF-012、SEC-001内部鉴权；随实际接入落实P04/P05/P06并补P11验证 | 独立测试集群真实执行；本地数据约束有效；镜像准备和Job分界清晰；稳定attempt可接管；脚本隔离；所接入接口最小认证/凭据配置/启动及进程和DB短时故障验证 |
| S5 边缘卸载与研究能力 | S4 | EDGE-001–002、OFF-001–002、WF-013、FL-001、MET-001；不含终端断线恢复 | 终端卸载边界正确；普通选址不冒用DQN；终端/策略统一执行；FedAvg/FedProx两轮产物与评估不串轮；开始实施时确定计量口径并可复核 |
| S6 P2和编辑管理完善 | S3，资源相关依赖S4 | WF-014–016；Webhook/Checks/SLA/afterExecution/文件/编辑协议 | 协议与存储版本明确；重投幂等；后处理失败不改主结果；编辑、预览、保存语义一致 |
| S7 数据迁移与上线验收 | S1–S6中确定的上线范围 | MIG-001、OPS-001；转换dry-run、旧任务排空、切换与回退；保障范围为P04/P05/P06/P11及P01/P07现状回归 | 无长期双模型；旧功能逐项回归；完成已选最小保障的验收并列明后置项，不把未测能力写成上线保证 |

P2 保留在路线中，是否作为首次上线前置条件可调整，但必须记录被后置的具体功能及原因。若没有真实多机环境，测试只能标注“多集群模拟”，不能声称完成真实多云故障验证。暂不承诺工期；S1/S2样板完成后再估算剩余工作量。

## S0 工作包

| 任务 | 产出 | 验收 |
|---|---|---|
| S0-01 | 独立父 POM、8模块和 package-info | 模块解析、依赖无环；没有旧父工程依赖 |
| S0-02 | 总览、Java索引、需求索引、计划、进度 | 必需文档齐全；计划和当前状态分离 |
| S0-03 | ADR、功能/验证模板、协议和迁移入口 | 从需求到代码与测试的关联规则明确 |
| S0-04 | 文档/结构检查和可选JavaHome构建脚本 | 检查通过；可用JDK21编译验证；不改系统JAVA_HOME |
| S0-05 | 本次验证记录、待决事项和下一步 | 明确无业务能力；不把Maven无测试成功视为功能验收 |

## 后续每次实现的工作循环

S1工作包：S1-01领域模型与JSON/YAML校验；S1-02不可变修订与并发保存；S1-03幂等提交、事务队列和顺序Log；S1-04HTTP、身份和查询；S1-05真实MySQL并发/重启验证、文档与代码索引。全部通过才能完成S1。

1. 从下一阶段选一个可验收工作包，不跨阶段默认扩展。
2. 先补功能规格及其对应 Java 文件/接口设计；重大取舍写 ADR。
3. 实现最小逻辑与必要鲁棒性；增添对应测试。
4. 更新协议、实际代码索引和功能状态。
5. 运行相关检查，记录环境、版本、通过/失败及尚未覆盖的项目。
6. 更新进度与下一步；发现范围变化时修订本计划。

## 计划调整规则

可调整顺序、拆分工作包、后置非必要功能。修改需写明原因、影响功能、依赖变化与新的验收条件。稳定阶段/功能编号不重复使用，不删除失败记录。不能通过缩减验收条件把未完成工作改成完成；范围裁剪必须明确标记。

当前已通过的阶段若因变更失效，应在进度中标为“需重新验证”，不能沿用旧验证结果。

## 变更记录

2026-09-11 / 0.25：UI-03a按用户要求直接删除常驻说明，不改为展开提示。只调整前端展示，保留真实状态与操作反馈；不扩展后端能力或部署授权。

2026-09-11 / 0.23：用户授权UI-02同源No-code及验收后只更新CEA前端，后端/数据库/算法集群不重启。不改变S7和后置研究能力范围；依赖、工作包与验收条件见本页当前授权。

2026-09-11 / 0.24：用户授权迁移已有后端支持的老页面功能并更新CEA前端。新增UI-03管理页及真实Registry/K3s浏览器验收；不扩展Java/API/表，不借用旧DTO，不导入旧数据，原S7退出条件不变。回读不足的部署编辑和没有新接口的旧操作明确保留为缺口。

| 日期 | 版本 | 变更 | 依据 |
|---|---|---|---|
| 2026-09-11 | 0.22 | 增加DEPLOY-01独立D盘Compose部署与FedAvg/FedProx运行验收；不含旧数据迁移和切换 | 用户独立部署与“迁移完后直接开始部署cea环境”授权 |
| 2026-09-11 | 0.21 | 增加UI-01独立前端最小闭环，复用S6接口并参考Kestra交互；以实际JAR浏览器联调验收，不迁移旧前端 | 用户本次授权 |
| 2026-09-10 | 0.20 | 授权S6-02–04，明确文件固定修订、统一门禁/认证Webhook、SLA告警和终态后处理的最小闭环及恢复验证 | 用户“继续完成S6剩下部分”；ADR-0019 |
| 2026-09-10 | 0.19 | 开始S6，拆分编辑/流程管理、Namespace Files、Webhook/Checks、SLA/afterExecution；先验收01。S5计量/DQN研究后置，不阻塞S6，不进入完整前端或S7 | 用户“先开始完成S6”；ADR-0018 |
| 2026-09-10 | 0.18 | S5-04c移除普通执行对卸载观测的读写和资源释放依赖，保留显式卸载；推进S5-05口径/协议确认，确认前不实现计量代码；完整选层拆分及多步DQN后置 | 用户“先做最小解耦，然后推进S5-05”；ADR-0017 |
| 2026-09-10 | 0.17 | S5-04b显式卸载、规则/单步Q、真实画像与反馈、终端FIFO；不新增执行链，不宣称长期DQN/性能结论 | 用户继续04剩余；ADR-0016 |
| 2026-09-10 | 0.16 | S5-04先打通终端Docker及可信来源，后接规则/DQN/画像；原卸载退出条件保留，不以本地容器运行冒充卸载完成 | 用户“继续S5-04”“建议终端安装docker”；ADR-0015 |
| 2026-09-10 | 0.15 | S5-03网关CONNECT、终端归属、策略管理隔离、事件触发及正常结果查询；复用Flow/Execution公开服务，增加edge→dataflow依赖，不进入卸载/计量 | 用户“S5-03”“继续”；ADR-0014 |
| 2026-09-08 | 0.1 | 建立S0–S7递进计划；首步仅代码和文档框架 | 用户当前要求 |
| 2026-09-08 | 0.1 | 采用8模块；卸载限定可卸载终端任务；取消按申报书硬分四模块 | 最近两轮职责讨论；ADR-0001 |
| 2026-09-08 | 0.2 | 用户授权执行S1，拆分S1-01至S1-05；采用Boot4.1.1/MySQL8持久Log，S2–S7未授权实施 | 用户“执行S1”；ADR-0003 |
| 2026-09-09 | 0.3 | 用户授权继续S2；拆分S2-01至S2-04：持久Worker、失败策略、Errors/Finally、故障验收。采用同库独立进程，未增加Broker/WorkerGroup；不扩展S3/S4 | 用户“继续S2”；ADR-0004 |
| 2026-09-09 | 0.4 | backend独立建仓；新后端Java21、技术版本以适用为准；明确裁剪终端断线恢复，不改变服务端Worker恢复；计量口径后置至S5开始；生产保障从笼统必做改为用户逐项选择，尚未确认的保障不得标为完成 | 用户本次六项决定；生产部署保障待选清单 |
| 2026-09-09 | 0.5 | 生产保障范围确认：P01/P07保留现状；P04/P05/P06/P11后续最小实现；P02/P03/P08/P09/P10/P12/P13现在后置。P010按P10归一；不删现有保护、不取消S3核心队列，不自动启动新阶段 | 用户逐项选择；生产保障范围清单 |
| 2026-09-09 | 0.6 | 授权S3-01至S3-04：嵌套控制、Flow并发FIFO、单Flow持久Cron、故障验收；用FlowExecutor替代顺序游标，不实现S4资源/容器或扩大生产保障范围 | 用户“开始S3”；ADR-0005 |
| 2026-09-09 | 0.7 | 授权S4，拆为S4-01资源目录/本地性、S4-02镜像契约/部署、S4-03观测/预约/Job、S4-04通用任务、S4-05部署故障验收；逐批验证，未完成批次保留，不将预览当真实调度 | 用户“开始s4”；ADR-0006 |
| 2026-09-10 | 0.8 | 授权S4-02，明确a契约/绑定、b镜像准备/分发、c常驻部署的验证次序；本批实现a，保留b/c和全S4退出条件，不增加不可执行Task或空Runner | 用户“开始S4-02”；ADR-0007 |
| 2026-09-10 | 0.10 | 用户授权完成S4剩余工作，按b/c、03、04、05逐批验收；显式Flow边界不变，不进入S5 | 用户“完成s4的剩下部分”；ADR-0008 |
| 2026-09-10 | 0.9 | 修正S4-02a为纯应用契约目录；撤销自动Flow Input派生、alias/fixedValues与plan/resolve。显式Task参数映射延后到真实Application/Container Task执行闭环；不进入b/c或S4-03，S4原退出条件保留 | 用户“S4-02a方向修正要求”；修订ADR-0007 |
| 2026-09-10 | 0.11 | 落实S4-03/04/05最小执行范围：平台Job槽、Ready节点与本地性、真实Job/文件、GET/POST、只读SQL、容器Shell/Python，以及进程/DB短时故障和实际JAR验收；明确不支持写SQL、任意镜像和跨地域容灾，不进入S5 | 用户“继续实现S4剩下部分”；ADR-0009/0010 |
| 2026-09-10 | 0.12 | 授权S5，拆分五个可验收批次，先Repeat轮次/反馈/屏障；网关、终端卸载、算法迁移与计量保持后续验收，不缩减S5总退出条件 | 用户“开始S5”；ADR-0011 |
| 2026-09-10 | 0.13 | 本批S5-02仅迁移FedAvg/FedProx，以真实两轮训练/加权聚合/全局评估及模型反馈验收；其他流任务暂不迁移，不进入网关/卸载/计量 | 用户“先做FedAvg和FedProx的迁移就行了，其它的流任务先不做” |
| 2026-09-10 | 0.14 | S5-02b通用Loop动态集合、item、并发/屏障/有序输出，动态candidateClusters和集合文件清单；两种客户端数量验证单份train；复用Repeat/TaskRun/Worker链，不进入03–05 | 用户“按这个方案修改”；ADR-0013 |
