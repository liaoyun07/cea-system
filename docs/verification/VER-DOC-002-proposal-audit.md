# VER-DOC-002 申报书功能审查表验证

日期：2026-09-14。类型：文档审查与进度映射，不是业务实现、部署或性能验收。代码基线为main `6bed211ddc561351671524f9683e7ad8c6c49415`，本记录与同批文档修改随后一并提交。

## 范围

- 新增[申报书功能审查表](../07-proposal-audit.md)：26项功能、8项指标/研究成果及8项范围差异。
- 更新README、总览、功能索引、实施计划0.42、当前进度与文档维护规则，提供入口和后续同批维护约束。
- 功能索引仍是实现状态汇总，审查表只保存原文覆盖判断，避免第二套事实源。
- 不新增/删除Java类、字段、表、API、配置、测试或执行链。不改变用户对DQN、计量、ARM、终端离线恢复及旧系统切换的取舍。

## 来源与核查

上一轮已完整提取项目根目录《课题三.docx》正文并核对原申报书。本次再次从原PDF核对课题三章节位置，读取任务分工指标，检查PDF55/56页完整页面图像，确认绝对指标、相对指标及20%/25%差异。保留来源定位，不把原始申报材料上传公开仓库。

逐项对照当前Flow模型、JobPlacementService、ImageDistributionService、网关上传、终端卸载协议，以及S3–S6、UI-08/09/10、FILE-01、EP-01/02的功能和验收记录。历史小节的“未实现”不得覆盖后续批次已完成事实。

## 检查结果

结果：PASS。

- `scripts/check-scaffold.ps1`：8模块、104生产Java索引、32功能编号及631个本地链接通过；模块依赖未变。
- 42个审查项ID唯一、引用均可解析：A8/B7/C4/D7共26个功能项，M8个指标/成果项，X8个范围项。
- 功能覆盖计数核对：11完备、10部分覆盖、5未实现；研究后置/范围待确认保留在相应行，没有转成“已完成”。
- 10张Markdown表列数一致；`git diff --check`通过。
- 修改范围核查仅8份Markdown文档，无生产代码、POM、测试、SQL或部署配置变化；分支main、origin为已授权的cea-system仓库。

以上仅为文档验证。未重跑Maven、浏览器或算法测试，既有240项Maven/57项浏览器回归仍是EP-02历史证据，不是本次重跑。

## 发布边界

纯文档批次无需构建镜像或重启CEA。按持续授权普通提交并同步既有GitHub main；无业务发布、DB migration、数据变更或旧系统操作。

## MET-PLAN优先级调整

日期：2026-09-14。代码与既有性能证据基线：`ae20e00d4cb7f95b5f98d2591eae1d8aa15266c9`。

按用户最新要求，仅同步实施计划0.47、当前进度、审查表和S5-05计量待办说明。固定性能指标按M04→M03→M01→M02推进，相对提升M05–M07后置；M08四模型是独立成果清单。保留原门槛、所有未验收状态、M02现有不达标结果及M01口径确认前置，不变更Java/API/数据库/配置/测试或CEA业务数据。

核查依据为现有S5-05候选计量、UI-08部署计时/资源采样协议和OFF-04真实实验记录；本次没有重新解读原文、修改验收门槛或重新运行性能实验。验证PASS：`scripts/check-scaffold.ps1`确认8模块、108份生产Java索引、32功能编号和716个本地链接；42个审查项ID唯一，10张审查表列数一致，`git diff --check`通过。差异仅5份Markdown文档。不重跑Maven/浏览器/算法，不构建镜像或重启CEA。
